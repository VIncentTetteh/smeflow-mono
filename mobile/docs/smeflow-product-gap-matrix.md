# SMEflow Backend Vs Mobile Product Gap Matrix

Status legend: `done` is backed by API or safe local offline flow and visible in mobile; `partial` exists but needs deeper UX/tests; `frontend-gap` has API support but mobile is missing or thin; `backend-gap` needs an API/contract; `future` is intentionally out of the current mobile scope.

| Story area | Backend source of truth | Mobile status | Offline status | Roles | Gap status | Test status |
| --- | --- | --- | --- | --- | --- | --- |
| Phone OTP registration/login | `POST /api/v1/auth/otp/request`, `POST /api/v1/auth/otp/verify` | Phone and OTP screens use stored phone through verification | none | all | partial | store + routing tests, needs E2E |
| Session bootstrap | `/auth/me`, `/auth/kyc/status`, `/auth/businesses`, `/business/me`, `/kyc/me`, `/business/momo-accounts`, `/business/members` | Root bootstrap now loads user, business list/current role, KYC, wallets, and members | persisted auth only | all authenticated | partial | API hook tests cover core fetch path, business profile, KYC mutations, wallets, and businesses |
| Logout | `POST /api/v1/auth/logout` | Owner/agent More has logout and clears scoped local data | clears local session + business-scoped local rows | all | done | hook UI test still needed |
| Business create/profile | `POST /business`, `GET/PATCH /business/me` | Business onboarding and profile edit include name, type, address, TIN, and Ghana Card ref | profile online-only | owner/manager | partial | hook mutation test added; screen test still needed |
| Business switch | `GET /auth/businesses`, `POST /auth/switch-business` | Settings lists active memberships, switches context, clears scoped local data, and re-bootstraps | clears business-scoped local rows | multi-business users | partial | settings/team screen tests added; native relogin smoke still needed |
| Staff invite/members | `GET /business/members`, `POST /business/members/invite`, `PATCH/DELETE /business/members/{member_id}` | Team workspace lists members from session bootstrap, invites manager/staff/agent, updates non-owner roles, and deactivates non-owner members | none | owner/manager | partial | screen coverage added; hook edge tests still needed |
| MoMo account linking | `/business/momo-accounts/*`, `/set-primary`, `/verify` | Onboarding can skip/link; Settings can add MTN/Vodafone/AirtelTigo, edit, set primary, and delete; hooks update local auth state after mutations | none | owner/manager; delete/set-primary owner-gated in mobile | partial | hook CRUD/primary tests added; screen/native wallet smoke still needed |
| User KYC | `/auth/kyc/status`, `/auth/kyc/submit` | status loaded; onboarding/settings can submit user KYC fields without blocking dashboard | none | all | partial | hook submit test added; screen/native KYC smoke still needed |
| Business KYC | `/kyc/me`, `/kyc/submit` | onboarding/settings can submit business KYC; dashboard prompts when not verified | none | owner | partial | hook submit test added; screen/native KYC smoke still needed |
| Inventory list/search/low stock | `/inventory/items`, `/inventory/categories`, sync pull | Owner Stock uses WatermelonDB real rows only with name/SKU/barcode search and category/low-stock filters | local-first | owner/manager/staff | partial | schema/sync mapper, local DB, and inventory screen tests added |
| Inventory add/edit/delete | `/inventory/items` CRUD | Stock screen supports add/edit/delete with unit, category, SKU, barcode, prices, current stock, and threshold | local-first with sync push create/patch/delete | owner/manager | partial | schema/sync mapper, local CRUD, and add-item screen tests added; edit/delete screen edges still need coverage |
| Stock adjustments | `POST /inventory/adjust`, local `stock_transactions` | adjustment modal supports purchase, damage, return, transfer, and correction with movement history | local-first, attempts online adjust for server-backed items | owner/manager/staff | partial | schema/sync mapper, API route, local fallback, and restock screen tests added |
| Low-stock WhatsApp/SMS alerts | `/inventory/restock-alerts`, `/analytics/inventory/alerts/low-stock`, `/analytics/inventory/low-stock`; provider send/status/retry contract not confirmed | mobile shows low-stock state and thresholds, but does not yet send WhatsApp/SMS alerts | local low-stock detection only | owner/manager | backend-contract-gap | minimum mobile/API contract documented in provider smoke checklist |
| Barcode/QR inventory scanning | `/inventory/items/lookup`, item barcode fields | Stock screen can scan into search and item barcode fields | local scan/search | owner/manager/staff | partial | scan-to-form/search screen tests added; physical camera smoke still needed |
| POS cash sale | `/sales/record`, local `sales/sale_items` fallback | Sell now records online first, then queues local fallback only when the backend path is unreachable or local inventory is unsynced | online-first with offline queue fallback | owner/manager/staff | partial | online-first helper, sync/local fallback, and POS cash screen tests added |
| Mixed/credit sale | sales schemas support split and credit payments; `/sales/{sale_id}/pay` records credit payments | Sell supports cash, MoMo, mixed cash+MoMo, credit, and explicit GhQR settlement mode through the same online-first sale helper | online-first with backend-compatible offline payloads | owner/staff | partial | split payload, idempotency, validation, and POS screen tests added |
| MoMo/GhQR payment request | `/sales/record` creates MoMo payment requests; `/sales/momo/intents/*` verifies Paystack prompt state; invoices expose GhQR payloads | Sell submits MoMo/mixed sales through backend sale recording and routes receipts to invoice workspace for GhQR/PDF details | queued only when offline/unreachable | owner/staff | partial | MoMo intent/check-payment tests added; provider reconciliation smoke still needed |
| Receipts/invoices | `/invoices/*`, `/invoices/by-sale/{sale_id}`, `/invoices/{invoice_id}/debit-note`, PDF/send endpoints | Sell shows receipt loading/ready/queued states; Invoices workspace lists/details/generates/sends/shares/voids/creates linked debit notes with GRA fields | online-only invoice workspace; offline sale receipt queues until sync | owner/manager | partial | backend invoicing tests + mobile bundle pass; screen tests still needed |
| Customers/receivables | typed `/sales/customers`, `/sales/customers/{id}`, `/sales/{sale_id}/pay` | Sales tab shows recent sales, customers, outstanding credit, customer detail, and payment recording | local customers table exists; receivable follow-up is online | owner/staff | partial | screen tests needed |
| Payroll | `/payroll/*` | employee list, add/edit/deactivate, period run picker, run history, payslips, and MoMo disbursement actions | none | owner/manager | partial | route contract test added; screen edge tests still needed |
| Tax | `/tax/*` | tax workspace summary, GRA draft refresh/file/export flow, input VAT capture, deadlines, and PDF share | none | owner/manager | partial | screen edge tests still needed |
| Credit scoring/loans | `/credit/score`, `/credit/request`, history/requests | score, history counts, loan confirmation, KYC gate | none | owner | partial | confirmation tests needed |
| Analytics/reporting | `/analytics/*`, `/sales/summary/daily` | dashboard uses daily summary and zero empty state | none | owner/manager | partial | loading/error tests needed |
| Chat assistant | `/chat/process`, `/chat/history` returns intent entities | chat confirmation records parsed item, qty, and price through the local sale writer | local confirmation for real item | owner/staff | partial | parser/action tests needed |
| USSD | `/ussd/callback`, `ENABLE_USSD`, `USSD_CALLBACK_SECRET`, `USSD_REDIS_DB` | no mobile screen by design | Redis-backed channel sessions | all | partial | backend integration tests cover callback, registration, sale, stock, summary, receivables, credit score; telco sandbox smoke still needed |
| Notifications/devices | `POST /auth/devices` | notification preferences screen and event list exist; device registration still needs native/dev-client validation | none | all | partial | screen coverage added; native/dev-client test needed |
| Referrals/subscriptions | `/referrals/my-code`, `/referrals/invite`, `/referrals/status`, `/billing/plans`, `/billing/subscription`, `/billing/subscribe`, `/billing/subscription/change`, `/billing/transactions`, `/billing/invoices` | Owner More exposes referrals and billing workspaces; referrals can invite; billing can view plans/history and start Paystack checkout/change plan | none | all | partial | API/domain/hook route tests added; provider checkout/cancel smoke still needed |
| Agent dashboard/pipeline/onboarding | `/agents/*`, `/agents/onboarding/*` endpoints | dashboard/pipeline use backend data; agent onboarding now follows start, business, wallet, KYC, complete flow | none | agent | partial | agent route tests needed |
| Admin/platform/fraud | admin/platform ownership not mobile-focused | not exposed in mobile | none | admin | future/backend-gap | admin web/API plan needed |

## Priority Fix List

- [x] Stop demo inventory from appearing as real business data.
- [x] Add session bootstrap state for user, business, KYC, wallets, and members.
- [x] Add logout and basic business/member account actions.
- [x] Fix route targets to concrete Expo paths (`/`, `/otp`, `/onboarding/*`, `/owner`, `/agent`).
- [x] Add remaining API hook tests for invite, profile update, member update/deactivate, wallet CRUD, KYC submit, and switch business/local reset-adjacent flows.
- [x] Replace Settings Team and Offline/Sync alert dead-ends with mobile workspaces.
- [x] Expand inventory to add/edit/delete, item unit/category/barcode fields, adjustment reasons, and backend create/patch/delete sync payloads.
- [x] Add online-first sale helper tests for backend-first recording, offline fallback, and validation-error handling.
- [x] Add POS screen tests for cart totals, receipt states, and MoMo pending. Empty inventory remains a small screen edge follow-up.
- [x] Build customer/receivable screens before invoice/payroll/tax depth.
- [x] Build invoice workspace with GRA fields, PDF/share/send, void credit note, and linked debit note actions.
- [x] Add backend multi-business list endpoint and wire mobile business switch UX.
- [ ] Run telco sandbox smoke for USSD callback signature and menu delivery once gateway credentials/short code are available.
- [x] Document provider/native smoke steps, evidence requirements, and low-stock WhatsApp/SMS alert contract.
